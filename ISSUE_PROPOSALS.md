# 코드베이스 이슈 분석 및 작업 제안

## 1) 오탈자 수정 작업 (Typos)
- **제안 작업:** CTA 섹션의 문구 `*얼리버드 전략 성공 기준`을 실제 맥락에 맞는 표현(예: `*얼리버드 선착순 예약 기준`)으로 수정합니다.
- **근거:** 가격 안내 문맥에서 `전략`은 오탈자/오용으로 보이며, 예약 기준 설명 문구와 의미가 어긋납니다.
- **완료 기준:** 가격 카드 하단 주석 문구가 사용자에게 명확한 예약 기준으로 읽히도록 교정됩니다.

## 2) 버그 수정 작업 (Bug Fix)
- **제안 작업:** HTML 문서 끝부분의 중복 닫힘 태그(`</body></html>`가 2회 존재)를 제거해 DOM 구조를 정상화합니다.
- **근거:** 문서 마지막에 닫힘 태그가 중복되어 있어 브라우저 파서 보정에 의존하며, 향후 스크립트/마크업 유지보수 시 예기치 않은 렌더링 이슈를 유발할 수 있습니다.
- **완료 기준:** 문서가 단일 `</body></html>`로 종료되고, HTML validator 기준으로 구조 오류가 제거됩니다.

## 3) 코드 주석/문서 불일치 수정 작업 (Comment or Docs Mismatch)
- **제안 작업:** `submitForm()` 함수 상단 주석의 `[추가된 부분]` 표현을 실제 역할 중심(예: `사전 예약 알림 제출 로직`)으로 변경합니다.
- **근거:** 현재 표현은 변경 이력 중심 주석으로, 코드의 기능을 설명하지 않아 시간이 지나면 맥락을 잃기 쉽습니다.
- **완료 기준:** 주석이 "무엇을 하는 코드인지"를 설명하도록 정리되고, 이력성 문구는 제거됩니다.

## 4) 테스트 개선 작업 (Test Improvement)
- **제안 작업:** 간단한 정적 검증 스크립트(예: Node/Python 기반)를 추가해 아래 항목을 CI 또는 로컬에서 자동 점검합니다.
  - 문서 종료 태그 중복 여부 검사
  - Open Graph 필수 메타 태그 존재 검사
  - 예약 입력 필드/버튼 ID 및 핸들러 연결 여부 검사
- **근거:** 현재 저장소는 단일 `index.html` 중심으로 동작하지만 자동 검증이 없어 사소한 회귀(태그 누락, 마크업 구조 깨짐)를 사람이 수동으로만 발견할 수 있습니다.
- **완료 기준:** `npm test` 또는 `python` 명령 1회로 핵심 구조 회귀를 탐지할 수 있습니다.
